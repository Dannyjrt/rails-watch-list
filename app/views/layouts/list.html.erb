<h1> Movies list </h1>

<%= cl_image_tag("the_image_i_added", width: 400, height: 150, crop: full)%>

<table>
  <thead>
    <th> Title </th>
    <th> Body </th>
    <th colspan="3"></th>
  </thead>

<tbody>
  <% @movies.each do |movie|%>
  <tr>
    <td><%= movies.title %></td>
    <td><%= movies.genre %></td>
  </tr>
  <% end %>
